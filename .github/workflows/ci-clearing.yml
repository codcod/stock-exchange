name: CI (clearing)

on:
  push:
    branches: [main]
    paths: ["platform/clearing/**", "platform/base/**", "uv.lock"]
  pull_request:
    paths: ["platform/clearing/**", "platform/base/**", "uv.lock"]

jobs:
  ci:
    uses: ./.github/workflows/service-ci.yml
    with:
      service: clearing
