# Application services — all eight microservices.
# Requires the 'exchange' network created by compose.infra.yml.
# Run with: just services-up   (after just infra-up)
#        or: just up            (starts infra + services together)

name: exchange

networks:
  default:
    name: exchange
    external: true

services:
  # ---------------------------------------------------------------------------
  # Tier 1: no service dependencies — only Postgres
  # ---------------------------------------------------------------------------

  account:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    command: python -m services.account
    environment:
      DATABASE_URL: postgresql+asyncpg://exchange:exchange@postgres:5432/exchange
      RISK_ENGINE_URL: http://risk-engine:8002
      PORT: "8006"
    ports:
      - "8006:8006"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8006/health')\""]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    depends_on:
      postgres:
        condition: service_healthy

  market-data:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    command: python -m services.market_data
    environment:
      PORT: "8005"
    ports:
      - "8005:8005"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8005/health')\""]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  # ---------------------------------------------------------------------------
  # Tier 2: clearing (trade record keeper — no account dependencies)
  # ---------------------------------------------------------------------------

  clearing:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    command: python -m services.clearing
    environment:
      DATABASE_URL: postgresql+asyncpg://exchange:exchange@postgres:5432/exchange
      PORT: "8004"
    ports:
      - "8004:8004"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8004/health')\""]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    depends_on:
      postgres:
        condition: service_healthy

  # ---------------------------------------------------------------------------
  # Tier 2: notifications (receives events from OMS and Matching Engine)
  # ---------------------------------------------------------------------------

  notifications:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    command: python -m services.notifications
    environment:
      DATABASE_URL: postgresql+asyncpg://exchange:exchange@postgres:5432/exchange
      PORT: "8007"
    ports:
      - "8007:8007"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8007/health')\""]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    depends_on:
      postgres:
        condition: service_healthy

  # ---------------------------------------------------------------------------
  # Tier 3: risk engine (warms account cache from Account service on boot)
  # ---------------------------------------------------------------------------

  risk-engine:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    command: python -m services.risk_engine
    environment:
      DATABASE_URL: postgresql+asyncpg://exchange:exchange@postgres:5432/exchange
      ACCOUNT_URL: http://account:8006
      PORT: "8002"
    ports:
      - "8002:8002"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8002/health')\""]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    depends_on:
      postgres:
        condition: service_healthy
      account:
        condition: service_healthy

  # ---------------------------------------------------------------------------
  # Tier 4: order management (reserves via Account service)
  # ---------------------------------------------------------------------------

  order-management:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    command: python -m services.order_management
    environment:
      DATABASE_URL: postgresql+asyncpg://exchange:exchange@postgres:5432/exchange
      PORT: "8001"
      RISK_ENGINE_URL: http://risk-engine:8002
      MATCHING_ENGINE_URL: http://matching-engine:8003
      ACCOUNT_URL: http://account:8006
      NOTIFICATIONS_URL: http://notifications:8007
    ports:
      - "8001:8001"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8001/health')\""]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    depends_on:
      postgres:
        condition: service_healthy
      risk-engine:
        condition: service_healthy
      account:
        condition: service_healthy
      notifications:
        condition: service_healthy

  # ---------------------------------------------------------------------------
  # Tier 5: matching engine (restores open orders from OMS on startup)
  # ---------------------------------------------------------------------------

  matching-engine:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    command: python -m services.matching_engine
    environment:
      DATABASE_URL: postgresql+asyncpg://exchange:exchange@postgres:5432/exchange
      PORT: "8003"
      CLEARING_URL: http://clearing:8004
      ORDER_MANAGEMENT_URL: http://order-management:8001
      MARKET_DATA_URL: http://market-data:8005
      ACCOUNT_URL: http://account:8006
      NOTIFICATIONS_URL: http://notifications:8007
    ports:
      - "8003:8003"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8003/health')\""]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    depends_on:
      postgres:
        condition: service_healthy
      order-management:
        condition: service_healthy

  # ---------------------------------------------------------------------------
  # Tier 6: HTTP gateway (public entry point)
  # ---------------------------------------------------------------------------

  gateway:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    command: python -m services.gateway
    environment:
      PORT: "8000"
      ORDER_MANAGEMENT_URL: http://order-management:8001
      RISK_ENGINE_URL: http://risk-engine:8002
      MATCHING_ENGINE_URL: http://matching-engine:8003
      CLEARING_URL: http://clearing:8004
      MARKET_DATA_URL: http://market-data:8005
      ACCOUNT_URL: http://account:8006
      NOTIFICATIONS_URL: http://notifications:8007
    ports:
      - "8000:8000"
    depends_on:
      order-management:
        condition: service_healthy
      matching-engine:
        condition: service_healthy
      market-data:
        condition: service_healthy
      account:
        condition: service_healthy
