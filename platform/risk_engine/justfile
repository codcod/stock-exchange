[group('qa')]
build:
    uv build --package risk_engine

[group('qa')]
test:
    uv run --package risk_engine pytest . || test $? -eq 5

[group('qa')]
lint:
    uv run ruff check .
    uv run ruff format --check .
